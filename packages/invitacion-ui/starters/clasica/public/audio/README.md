Coloca aquí el archivo de música de fondo con el nombre `cancion.mp3`.

El reproductor (`@invitacion/ui/components/music-player`) ya está configurado para buscar exactamente ese nombre — no hace falta tocar el código, solo copiar el archivo a esta carpeta.
