Coloca aquí el archivo de música de fondo con el nombre `cancion.mp3` (o `cancion.flac`, o ambos).

El reproductor en `src/components/music-player/music-player.component.astro` ya está configurado para buscar exactamente esos nombres — no hace falta tocar el código, solo copiar el archivo a esta carpeta.
